You are writing engaging tweets about code bugs and features. Your tweets MUST be 240-280 characters.

**EXAMPLES (Study these - they are all 240-280 chars, NO EMOJIS):**

Example 1 (268 chars):
"Ever watched your app slowly die under heavy traffic? Found a memory leak where DB connections weren't being released on errors. Each failed query ate one connection. Fixed with proper cleanup logic + added timeout monitoring. Crisis averted! #bugfix #python #opensource"

Example 2 (259 chars):
"Race condition nightmare in our payment system. Two users could get charged for the same transaction because of missing synchronization. Added proper locking to serialize access. No more accidental double charges! #concurrency #bugfix #python"

Example 3 (264 chars):
"Massive performance win today. Switched from nested loops to hash map lookups in the rendering pipeline. Frame times dropped from 50ms to 20ms - that's a 60% speedup! Users are loving the smoothness now. #optimization #performance #engineering #opensource"

---

**YOUR TURN:**
Write a tweet about the following GitHub event. Make it 240-280 characters (study the examples above for length reference).

{input_data}

**STYLE RULES:**
- Tell a STORY (the problem, the impact, the solution)
- Be dramatic and engaging ("nightmare", "crisis", "massive win")
- NO file paths or code locations (just describe the issue)
- NO EMOJIS (plain text only)
- Include hashtags: #bugfix #python #opensource #engineering
- Make developers feel the pain and the victory

**IMPORTANT:** 
- Output MUST be 240-280 characters total
- If it's trivial (typo/formatting), return "SKIP"
- Return ONLY the tweet text
- DO NOT include character counts like "(280 chars)"
- DO NOT include quotes around the tweet
- Just the plain tweet text, nothing else

Return ONLY the tweet text.
